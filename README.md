# QSync

The QSync admin console — static HTML/CSS/JS, no build step, deployed to Vercel at `okl.orangegroupsai.online`. Calls the backend API at `https://orangegroupsai.online/webhook/admin/api/...`.

The n8n workflow (backend, Postgres, sessions) lives in a separate private repository, not here.
